spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/enterprise-platform

# URL per chiamare il core-service dal tuo PC
app:
  core-service-url: http://localhost:8080